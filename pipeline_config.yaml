# Segmentation sapience
segmentaion:
  service_url: http://127.0.0.1:7860
  pre_preservation_classes:
    - Left_Hand
    - Right_Hand
  force_preservation_classes:
    - Hair
  soft_preservation_classes:
    - Apparel
    - Left_Hand
    - Left_Lower_Arm
    - Left_Upper_Arm
    - Right_Hand
    - Right_Lower_Arm
    - Right_Upper_Arm
  clothing_classes:
    - Upper_Clothing
    - Lower_Clothing

inpainting_diffusion:
  model_path: "/workspace/VTO_demo/ckpt/stable_diffusion/realvisxlV50_v40Bakedvae.safetensors"
  dtype: "torch.float16"  # Supported values: torch.float32, torch.float16, etc.
  neg_prompt: "(deformed iris, deformed pupils, semi-realistic, cgi, 3d, render, sketch, cartoon, drawing, anime:1.4), text, out of frame, worst quality, low quality, jpeg artifacts, ugly, duplicate, morbid, extra fingers, mutated hands, poorly drawn hands, poorly drawn face, mutation, deformed, (blurry:2.0), blur, dehydrated, bad anatomy, bad proportions, extra limbs, cloned face, disfigured, gross proportions, malformed limbs, missing arms, missing legs, fused fingers, too many fingers, long neck"
  inpaint_timestep_num: 50
  guidance_scale: 4.0
  strength: 0.99
  max_timestep: 500


# Device settings
device: cuda

# Paths
base_path: yisol/IDM-VTON
example_path: ./example

